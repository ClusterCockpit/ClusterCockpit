{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('css/datatables.min.css') }}" rel="stylesheet" type="text/css" />
    {{ parent() }}
{% endblock %}

{% block javascript %}
    {{ parent() }}
    <script src="{{ asset('js/datatables.min.js') }}"/></script>
    <script src="{{ asset('js/plotly-basic.min.js') }}"/></script>
    <script>
        {{ include('js/initDatatables.js.twig') }}
    </script>
{% endblock %}


{% block title %}
    Job List
{% endblock %}

{% block helptext %}
    <h4>Sorting</h4>
    <p>
        You can change the sort order of jobs by pressing the
        sort button in the first column and selecting a column.
        Press multiple times to switch between ascending and
        descending sort order.
    </p>
    <h4>Legend</h4>
    <p>
        Different lines represent nodes within a job. The colors of lines only
        helps to distinguish lines and does not serve another purpose. Metrics are
        aggregated values per node. The horizontal black line
        indicates an acceptable normal performance and serves as
        frame of reference.
    </p>
{% endblock %}

{% block body %}
    {% set message="No data " %}
    {{ include('util/printInfo.html.twig') }}
    {{ include('renderJobListAjax.html.twig') }}
{% endblock %}
