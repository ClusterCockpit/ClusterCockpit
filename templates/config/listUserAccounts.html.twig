{% extends 'base.html.twig' %}

{% block title %}
    Config Panel
{% endblock %}

{% block javascript %}
    {{ parent() }}
    <script src="{{ asset('js/feather.min.js') }}"/></script>
    <script>
        feather.replace()
    </script>
{% endblock %}

{% block body %}
    <div class="row">
        {{ include("renderSidebar.html.twig") }}
        <div class="col-10 ml-auto">
            <div class="table-responsive">
                <table class="table table-striped table-bordered  table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">Id</th>
                            <th scope="col">Name</th>
                            <th scope="col">Email</th>
                            <th scope="col">Active</th>
                            <th scope="col">Roles</th>
                            <th scope="col">Edit</th>
                            <th scope="col">Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for user in users %}
                            <tr>
                                <th scope="row">
                                    {{ user.id }}
                                </th>
                                <td>{{ user.username }}</td>
                                <td>{{ user.email }}</td>
                                <td class="text-center">
                                    {{ user.isActive ? 'Yes':'No' }}
                                </td>
                                <td>
                                    {% for role in user.roles %}
                                        {{ role }}</br>
                                    {% endfor %}
                                </td>
                                <td class="align-middle text-center">
                                    <a class="btn btn-success" href="{{ path('edit_user_account', {id : user.id}) }}" aria-label="Edit">
                                        <span class="fas fa-pen-square fa-lg" aria-hidden="true"></span>
                                    </a>
                                </td>
                                <td class="align-middle text-center">
                                    <a class="btn btn-danger" href="{{ path('delete_user_account', {id : user.id}) }}" onclick="return confirm('Are you sure? This cannot be undone!')" aria-label="Delete">
                                        <span class="fas fa-times fa-lg" aria-hidden="true"></span>
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
{% endblock %}

